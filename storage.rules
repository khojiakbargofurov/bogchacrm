rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      // For MVP, require authentication for uploads/downloads
      allow read, write: if request.auth != null;
    }
  }
}
